name: Legacy Transactions Security

on:
  pull_request:
    paths:
      - "convex/transactions.ts"
      - "scripts/legacy-transactions-security-check.mjs"
      - ".github/workflows/legacy-transactions-security.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  security:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout exact PR head
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Verify legacy transaction authorization boundaries
        run: node scripts/legacy-transactions-security-check.mjs
