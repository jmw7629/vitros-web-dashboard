name: Verify Secure Role Session #140

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  verify-secure-role-session:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Verify exact implementation SHA
        run: node scripts/verify-secure-role-session-140.mjs
