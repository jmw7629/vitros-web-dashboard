name: Legacy REM Read Security

on:
  pull_request:
    paths:
      - "convex/remAnalyzers.ts"
      - "convex/remLvcc.ts"
      - "convex/remWeeklyNotes.ts"
      - "convex/remStaffing.ts"
      - "convex/remTargets.ts"
      - "scripts/legacy-rem-read-security-check.mjs"
      - ".github/workflows/legacy-rem-read-security.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  security:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout exact PR head
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Verify legacy REM read authorization boundaries
        run: node scripts/legacy-rem-read-security-check.mjs
