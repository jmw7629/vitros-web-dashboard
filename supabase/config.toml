# Per-function runtime configuration.
# browser-safe-read is intentionally public and returns only explicit field/dataset allowlists.
# The browser helper sends no Authorization header, so JWT verification must be disabled
# for this function or Supabase will reject every request before the handler executes.
[functions.browser-safe-read]
verify_jwt = false
