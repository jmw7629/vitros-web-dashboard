# Per-function runtime configuration.
# browser-safe-read is retired. Production browser data must flow through the
# authenticated Convex server boundary; JWT verification remains enabled as an
# additional fail-closed layer on the tombstone function.
[functions.browser-safe-read]
verify_jwt = true
